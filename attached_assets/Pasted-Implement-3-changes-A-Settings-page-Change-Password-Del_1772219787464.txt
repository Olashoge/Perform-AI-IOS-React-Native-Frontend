Implement 3 changes:

A) Settings page: Change Password + Delete Account
1) Add a “Security” section on Settings.
2) Change Password:
   - Only show for email/password accounts.
   - Fields: currentPassword, newPassword, confirmNewPassword.
   - Validate client-side (required, new passwords match, minimum length).
   - Call backend endpoint to update password.
   - Show success + proper error messaging.
   - If user signed in via Apple/Google, hide/disable and show message “Password is managed by your sign-in provider.”
3) Delete Account:
   - Add “Danger Zone” section with “Delete Account” button.
   - On tap, show confirmation modal requiring typing DELETE (and/or password for email users).
   - Call backend delete endpoint.
   - After success: clear auth/session and route to auth screen.
4) Backend:
   - Add endpoints:
     - POST /auth/change-password (or /user/change-password)
     - DELETE /user (delete current user)
   - Ensure delete removes or cascades deletes related user data (profiles, plans, logs/completions, etc.).
   - Return clear error codes/messages.

B) Fix pills toggle selection everywhere (especially Profile page)
1) Audit all pill components and selection handlers.
2) Ensure every pill toggles:
   - Multi-select: clicking selected value removes it from array; clicking unselected adds it.
   - Single-select: clicking selected value again sets it to null (unselect).
3) Ensure UI selected state is derived from the same state (no stale local state).
4) Apply globally so all pills in the app behave consistently.

C) Theme behavior: Default to System until user overrides
1) Implement themePreference with 3 states: "system" (default), "light", "dark".
2) If preference is "system", theme follows OS/system scheme.
3) If user sets light/dark in Settings, persist override and apply it.
4) Add Settings UI control with options: System, Light, Dark.
5) Ensure app loads theme correctly on startup and respects changes instantly.

Do not redesign screens. Implement the features cleanly with existing styling and architecture.